# RAG server only — used by Railway deployment
# For full pipeline deps (docling, tesseract, torch etc.) see requirements.txt
fastapi>=0.110.0
uvicorn>=0.29.0
rank-bm25>=0.2.2
google-genai>=1.0.0
anthropic>=0.25.0
python-dotenv>=1.0.0
