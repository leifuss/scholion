# Zotero Connection
# Local mode (default) — requires Zotero desktop running:
ZOTERO_LOCAL=true
# ZOTERO_GROUP_ID=5166884            # optional: use a group library locally

# Web API mode — for CI/CD, headless sync, and multi-collection discovery:
# ZOTERO_LOCAL=false
# ZOTERO_API_KEY=your_key_here       # create at https://www.zotero.org/settings/keys
# ZOTERO_LIBRARY_ID=5166884          # group ID (or user ID for personal libraries)
# ZOTERO_LIBRARY_TYPE=group          # 'group' or 'user'
# ZOTERO_USER_ID=                    # optional: auto-detected from API key if omitted

# Google Cloud Vision (for Phase 2)
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json
# Or:
# GOOGLE_CLOUD_PROJECT=your-project-id

# LLM keys for RAG chat server (at least one required for chat; Gemini preferred)
# GEMINI_API_KEY=your_key_here
# OPENAI_API_KEY=your_key_here
ANTHROPIC_API_KEY=your_key_here

# Notifications → iPhone / Apple Watch (free, via ntfy.sh)
# 1. Install the 'ntfy' app on iPhone and subscribe to your topic
# 2. Set a unique hard-to-guess topic name below
# NTFY_TOPIC=leifuss-pipeline-changeme

# Processing Settings
# The Zotero group is "Islamic Cartography"; the collection inside it
# was auto-named by a CambridgeCore batch import.
COLLECTION_NAME=CambridgeCore_Citation_04Nov2025
MAX_WORKERS=4
